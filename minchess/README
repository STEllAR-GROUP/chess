To compile the serial version type: make

To clean up the code type: make clean

This minchess build has removed all command line parameters and set them to their default
values in order to make testing easier (will reimplement them when the need arises)

To run type
./minchess


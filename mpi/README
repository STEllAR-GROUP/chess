To compile, type 'make'

To remove the binary file, type 'make clean'

To run (MPI only) type:

mpirun -np <number of processors> mpichess <depth of white> <depth of black>

ex.

mpirun -np 4 mpichess 10 3
